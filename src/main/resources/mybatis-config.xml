<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <properties resource="database.properties" />
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
    <typeAliases>
        <typeAlias alias="Player" type="com.example.model.Player"/>
        <typeAlias alias="Club" type="com.example.model.Club"/>
        <typeAlias alias="PlayerClub" type="com.example.model.PlayerClub"/>
        <typeAlias alias="Country" type="com.example.model.Country"/>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${db.driver}"/>
                <property name="url" value="${db.url}"/>
                <property name="username" value="${db.username}"/>
                <property name="password" value="${db.password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/PlayerMapper.xml"/>
        <mapper resource="mapper/ClubMapper.xml"/>
        <mapper resource="mapper/PlayerClubMapper.xml"/>
        <mapper resource="mapper/CountryMapper.xml"/>
    </mappers>

</configuration>